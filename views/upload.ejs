<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload</title>
    <%- include('../parts/imports.ejs') %>
</head>
<body>
    <%- include('../parts/header.ejs')%>
    
    <div class="uploadClass">
        <div class="sideNavbar">
            <button id="addType">Add Type</button>
            <button id="addCategory">Add Category</button>
            <button id="addSubCategory">Add Sub-Category</button>
            <button id="addItems">Add Items</button>
        </div>
        <div class="otherSideOfNavbar">
            <div id="addTypeInputFeild" class="addTypeClass">
                <input placeholder="Add Type">
                <button>Add</button>
            </div>

            <div class="addCategoryClass">

            </div>
        </div>
    </div>
    


    <!-- <section id="uploadimg">
        <div>
            <svg  aria-hidden="true" xmlns="http://www.w3.org/2000/svg"  fill="none" viewBox="0 0 20 16">
                <path stroke="#0b7a55" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 13h3a3 3 0 0 0 0-6h-.025A5.56 5.56 0 0 0 16 6.5 5.5 5.5 0 0 0 5.207 5.021C5.137 5.017 5.071 5 5 5a4 4 0 0 0 0 8h2.167M10 15V6m0 0L8 8m2-2 2 2"/>
            </svg>
            <p>Click to upload or drag and drop</p>
            <p>PNG, JPG(only)</p>
        </div>
        <div>
            
        </div>
    </section> -->

    


    <%- include('../parts/footer') %>


    <script>
        let isType=0,isCategory=0,isSubCategory=0,isItems=0;
        const addTypeBtn = document.getElementById("addType")   
        const inputTypeFeild = document.getElementById("addTypeInputFeild")
        
        
        addTypeBtn.addEventListener("click",()=>{
            console.log("addType is Clicked");
            addTypeBtn.classList.toggle("afterTheClicked")
            if(isType==0){
                isType=1;
                // addTypeBtn.classList.add("afterClicked")
                inputTypeFeild.style.display='none'
            }
            else{
                isType=0;
                // addTypeBtn.classList.remove("afterClicked")
                inputTypeFeild.style.display='flex'
            }
        })

    </script>
</body>
</html>